# VerifyOS V1 MVP 演示动线脚本

> 场景：向领导/团队演示 VerifyOS（中文 AI 测试平台）的核心能力。
> 入口：`http://localhost:5173`（全栈常驻）· 时长 ~10 分钟 · 截图引用 `shots/*.png`
> 覆盖能力：E 系全部 + F 系全部（含 F4-deep 接管、F11-dyn PR 动态探索）

---

## 开场（30 秒）

**讲**：「这是 VerifyOS——一个 AI 驱动的中文测试平台。核心理念是『测试，从一句话开始』：你描述业务，AI 自动探索应用、生成 QA 点、执行验证、产出证据，失败自动归因到根因，PR 变更自动做探索式回归。」

**看**：侧边栏 12 个模块（概览/AI 工作区/探索/需求导入/QA 点/验证编辑器/验证执行/执行历史/应用地图/问题/PR 验证/工具与插件/移动测试/凭据）——全部真数据真功能。

---

## 1. 探索闭环：一句话跑通全链路（1.5 分钟）

**点**：「AI 工作区」→ 底部点快捷 chip「帮我探索演示站，关注员工管理权限」

**讲**：「我只说一句话，AI 自动规划：爬取应用 → 发现登录墙 → 用凭据自动登录 → 生成应用覆盖图 → LLM 从图中提取 QA 点候选，全部落库。」

**看**：AI 回复 + 🌐 探索任务卡 → **~30 秒后自动弹出「🗺 探索完成 · 结果统计」卡**（页面节点/跳转边/QA 候选三个大数字 + 触达页面清单）——**不用猜等待时间，完成事件真实驱动** → 链式弹出「建议的 QA 点」勾选卡，勾选即批量生成验证。

**切**：「应用地图」→ SVG Coverage Graph（Intent 分档着色 + ✓ 已触达 / ✕ 失败标记 + QA 徽标 + 点节点看详情）。

**截图**：`shots/chat-explore-done.png`（探索完成统计卡）、`shots/pg-map.png`（应用地图）

---

## 2. 探索屏：Live Findings + 人工接管（1.5 分钟，全场高光）

**点**：「探索」→ 选「Headful（可接管）」→「▶ 开始探索」

**讲**：「探索途中 Agent 会即时报告故障信号——HTTP 错误、慢响应、JS 控制台错误、登录墙，红黄分级出卡，这就是 **Live Findings**。」

**看**：左列探索路径树实时生长 + 中列「Agent 正在做什么」+ 右侧 Live Findings 卡（发现即可「加入 QA 点」）。

**人工接管**（重点演示）：「遇到验证码/扫码/复杂登录怎么办？——点『👤 我来操作』，Agent 暂停，你在浏览器里手动完成登录，**你访问过的页面会被自动记住**；点『交还』，Agent 从你停下的位置继续爬——人过墙，Agent 接管墙内世界。」

**看**：接管横幅（含 chrome://inspect 接入指引）→ 交还后活动日志出「[接管] 人工访问 N 个页面已并入探索队列」→ 路径树继续生长墙内页面。

**截图**：`shots/explore-takeover.png`（接管横幅）、`shots/live-findings.png`（故障信号卡）

---

## 3. 需求导入：PRD 直接变成测试资产（1 分钟）

**点**：「需求导入」→「载入样例」（或上传 .docx——浏览器端 mammoth 真解析）

**讲**：「PRD 文本自动结构化拆分：功能域、角色、规则、验收标准、状态机——还有**模块×角色矩阵**（每个角色在每个模块下有几条规则）。」

**点**：「🔍 交叉验证」→「需求规则与探索图/QA 点库三方对碰，缺口即出卡：需求有系统未见、图有 QA 缺、实现超出需求声明。」

**看**：红/黄疑点卡 → 三个动作全是真的：「转 QA 点」落库 ·「忽略」**持久化**（下次自动过滤）·「标记给产品」**真入问题库**。

**截图**：`shots/import-matrix.png`（模块×角色矩阵 + 交叉验证疑点）

---

## 4. QA 点 → 一键生成验证（1 分钟）

**点**：「QA 点」→ 风险筛选/搜索 → 勾选多条 →「⚡ 批量生成验证」（或单条「✨ 生成验证」）

**讲**：「QA 点不是死数据——一键变成可执行验证（前置登录 + 断言 + targetRef 防假绿声明）。这就是『Safe Mutation』：AI 建议，人确认，才落库执行。」

**看**：状态机流转（discovered→selected→generated）·「✓ 确认」按钮 · 生成后 alert 带 runId。

**截图**：`shots/qa-gen.png`

---

## 5. 验证执行：真浏览器 + 证据 + 防假绿（2 分钟）

**点**：「验证·执行」→ 右上「重新运行」

**讲**：「真浏览器执行：确定性登录（零 LLM）→ 断言 → AI 操作（Stagehand×glm-4.5v）→ 断言。全程事件流实时可见——thinking、action（带 LLM 计数）、observation、证据截图。」

**看**：三列结构——左步骤定义（**可编辑**：改标题/指令/断言/targetRef，增删步骤，拖拽排序，存为验证）· 中 Action Log · 右浏览器舞台真实截图。

**强调**：「核心卖点 **UNKNOWN ≠ PASS**——步骤全绿但没触达目标分支？判 UNKNOWN 不允许假绿。」

**构造演示**（可选）：改一步 targetRef 为不存在的分支 → 重跑 → **Triage 自动弹出失败分析**（判定 UNKNOWN + 失败时截图 + 证据列表）→「→ 转 issue」一键入问题库。

**截图**：`shots/triage-shown.png`

---

## 6. 执行历史：回放 + 报告导出（1 分钟）

**点**：「执行历史」→ verdict 筛选 tabs → 点任意一行

**讲**：「每个 Run 的全部事件流落库——点一下完整回放：步骤状态、AI 思考、动作、断言、证据、Triage 还原，重启不丢。每行还能看到关联的验证名称。」

**报告导出**（新）：「每个 Run 还能**一键导出 Markdown 报告**——判定表、步骤明细（含每步 LLM 次数/耗时）、触达校验、触达 URL、证据清单。历史归档的 Run 也能从事件流重建报告。直接 `GET /api/runs/:id/report` 下载。」

**演示**（curl）：`curl -O http://localhost:8082/api/runs/<runId>/report` → 打开 .md 文件。

**截图**：`shots/replay-live.png`（回放）、`shots/run-report.png`（Markdown 报告）

---

## 7. PR 验证：影响分析 + 定向回归 + 动态探索 + 门禁（1.5 分钟，技术高潮）

**讲**：「MR 打开时 webhook 自动触发四件事——①**影响分析**：LLM 读 diff 找出受影响业务域（风险分级+引用 diff 证据）②高风险自动落问题库 ③生成定向回归步骤（带 targetRef 防假绿）④**动态探索**：Agent 对 Preview 环境做一次 mini 探索，把 HTTP 错误/慢响应/JS 错误/登录墙这些故障信号直接挂上 MR。」

**演示**（curl）：`POST /api/webhooks/gitlab`（合成 MR  payload）→ 返回 impact + runId + `dynamicExplore: started`。

**看**：「PR 验证」→ 点 MR 进详情：SUMMARY / AREAS FOR IMPROVEMENT / **动态探索新发现**（约 50 秒后出现：故障信号红 chip、风险信号黄 chip，逐条「转 QA 点」）/ TESTS RUN / VerifyOS Bot 评论卡 + **合并门禁**（fail 阻止 / unknown 警告）→「回写 MR 状态」（stub 落盘 out/mr-comments/，token 接入后真发）。

**截图**：`shots/pr-dynamic.png`（动态探索新发现卡）

---

## 8. 移动测试 + 工具插件（30 秒）

**点**：「移动测试」→ 选「iPhone 13」→ 跑验证 → 看设备视口截图卡。

**点**：「工具与插件」→ 8 个内置工具卡（db.query 只读强制 LIMIT / db.exec 需人工批准 / http / browser…）+ 权限三档说明 + **调用审计列表**（谁在何时调了什么，auto/approved/denied 全留痕）。

**讲**：「执行引擎可扩展——数据库断言、HTTP 探针、视觉模型都走 ToolRegistry 插件化接入，权限三档（auto/ask/forbidden）+ 全量审计。」

**截图**：`shots/mobile-result.png`、`shots/plugins.png`

---

## 9. 私有化部署：30 分钟起服（30 秒）

**讲**：「单 compose 全栈（PG/Redis/server/web）——干净机器 `docker compose -f docker-compose.prod.yml up -d --build` 30 分钟起服。**容器自带 Chromium**，爬取/执行/移动模拟全在容器内真实运行，不依赖宿主机。凭据 AES-256 加密、权限三档、审计全量。」

**看**：当前演示环境就是这套 compose（`localhost:5173` 容器内 web→server→PG 完整链路，restart 自愈）。

---

## 收尾（15 秒）

**讲**：「总结：VerifyOS 把『需求→探索→生成→执行→证据→归因→回放→报告→PR 门禁』串成了一条 AI 驱动的测试流水线——30+ 张工单全部落地，五引擎（探索/执行/对话/PR 影响分析/动态探索）真实运行，可私有化部署。下一步：GitLab 真实回写、移动原生 App driver、定时回归。」

---

## 备用问答（FAQ 提示）

| 可能的问题 | 答 |
|---|---|
| LLM 用的什么模型？ | glm-4.5v（国产模型，A5 PoC 实测 4/4 门禁通过；可切 DeepSeek，LLM Gateway 统一入口） |
| 和 qa.tech 什么关系？ | 对标产品——核心能力对齐（探索/执行/PR 验证/移动/人工接管/动态探索），加上国产模型、GitLab/禅道生态、私有化部署、UNKNOWN 防假绿四个差异化 |
| 数据安全？ | 凭据 AES-256-GCM 加密、工具权限三档（auto/ask 人工批准/forbidden）、全量审计、私有化部署零外部 SaaS 依赖 |
| 准确率如何？ | 混合执行模型：确定性步骤零 LLM（快且稳），AI 只做规划和兜底（Stagehand 指令遵循 PoC 实测 ≥80%）；UNKNOWN 防假绿兜底防误判 |
| 探索到一半人能介入吗？ | 能——人工接管模式：暂停后人在真实浏览器里操作（过验证码/登录/切租户），访问过的页面自动并入探索队列，交还后 Agent 从人的位置继续 |
| 需求文档能直接进来吗？ | 能——.docx 真解析（mammoth）+ 结构化拆分（模块×角色矩阵）+ 与探索图/QA 点库交叉验证找缺口；Figma/飞书连接器凭据配好即真连 |
