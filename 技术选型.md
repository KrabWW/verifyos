# VerifyOS 技术选型文档 V0.2

> 配套文档：《PRD.md》（产品需求）、`prototype.html`（高保真原型）
> 本文档回答："每个技术决策选什么、为什么、备选是什么、怎么验证（PoC）、什么情况下换。"
>
> **V0.2 变更记录（2026-09-03）**
> 1. 前端框架 Vue 3 → **React 18**：AI 时代的会话/生成式 UI 组件生态集中在 React（assistant-ui、Vercel AI SDK UI 等），官方组件可直接复用，避免等社区 Vue 移植
> 2. 新增 **AI 会话层选型：@assistant-ui/react**（MIT），对齐原型已验证的 Thread 交互范式
> 3. 新增 **移动执行层**（2.11）：对齐 QA.tech 第四个产品演示（Test mobile apps），列为 Phase 5+ 扩展

---

## 0. 选型总原则

1. **组装优于自研**：第一版不发明 Agent 引擎、不训练模型、不自研图数据库
2. **单体优先**：一个后端服务 + 一个前端，禁微服务、禁 Kafka/Neo4j/Temporal/LangGraph 起步
3. **可替换分层**：Agent 层、浏览器层、LLM 层都要留接口缝，能换实现
4. **License 干净**：所有核心依赖必须 MIT/Apache-2.0；AGPL 仅可作参考不可作依赖
5. **私有化友好**：全组件可内网部署，不依赖海外 SaaS

---

## 1. 总体架构与分层

```
┌──────────────────────────────────────────────────┐
│ 前端  React 18 + TS + Tailwind + shadcn/ui        │
│      @assistant-ui/react（AI 会话）               │
│      AntV X6（图）· Tiptap（编辑）                 │
├──────────────────────────────────────────────────┤
│ 接口  REST + WebSocket（Agent 事件流）             │
├──────────────────────────────────────────────────┤
│ 后端单体（Node 22 + Fastify 或 NestJS）            │
│  ├ Requirement Engine   文档/原型理解              │
│  ├ Discovery Engine     爬取/探索/建图            │
│  ├ Agent Runtime        规划/执行/自愈            │
│  ├ Verification Engine  Oracle 判定              │
│  └ Evidence Engine       采集/存储/索引           │
├──────────────────────────────────────────────────┤
│ 执行层  Playwright（唯一浏览器底座）                │
│         Stagehand（Agent 抽象）                    │
├──────────────────────────────────────────────────┤
│ 存储  PostgreSQL(+pgvector) · Redis · MinIO        │
│ 观测  Langfuse（LLM）· OTel（系统）                │
└──────────────────────────────────────────────────┘
```

**关键决策 D1：后端语言**

| 选项 | 优势 | 劣势 | 结论 |
|---|---|---|---|
| **Node 22 + NestJS** ✅ | 与 Stagehand/Playwright 同语言同进程，无跨语言调用；前端团队可全栈 | CPU 密集任务弱 | **首选**。执行层是 Node 生态主场 |
| Python + FastAPI | AI 生态熟 | Playwright/Stagehand 都是 JS 库，跨语言桥接成本高且脆 | 备选（仅当团队 Python 强 JS 弱） |
| Java + Spring Boot | 企业集成熟 | 与执行层生态完全脱节，Agent 迭代慢 | 不推荐做执行层；可做对接层（Phase 4 与禅道/Jira 集成） |

**决策 D2：单体拆分时机** —— Run 执行器长任务多，第一版用 BullMQ（Redis）做任务队列即可；当探索任务并发 > 20 或单任务时长 > 30 分钟时，把 Execution Worker 拆成独立进程（同仓库 monorepo 包，非独立服务）。

---

## 2. 逐层选型

### 2.1 浏览器执行层 —— Playwright（无争议）

| 对比项 | Playwright | Selenium | Puppeteer |
|---|---|---|---|
| Trace/Video/Network/Console | ✅ 全内置（Run Viewer 证据直接来源） | 弱 | 无 trace |
| storageState（登录态复用） | ✅ | 需自建 | 部分 |
| 多浏览器 | ✅ | ✅ | Chromium only |
| A11y Tree | ✅（ariaSnapshot） | 间接 | 间接 |

**结论**：Playwright 唯一底座。Momentic Web runtime 也是基于它，证明路线可行。
**依赖的杀手级能力**：`trace.zip`（Run Viewer 回放）、`storageState`（Browser State 对象）、`route()`（Network 证据/ mock）、CDP（Console 采集）。

### 2.2 Browser Agent 层 —— Stagehand 首选

| 对比项 | Stagehand | Browser Use | Skyvern | 自研 |
|---|---|---|---|---|
| License | **MIT** ✅ | MIT | **AGPL-3.0** ⚠️ | — |
| 抽象 | act/observe/extract 正合验证语义 | 通用 Agent loop | 通用 workflow | 全自研成本高 |
| 缓存/自愈 | ✅ 内置（对齐 Momentic V3 思路） | 弱 | 部分 | 要自己造 |
| 嵌入产品 | TS 库，可直接 import 进后端 | Python 为主 | 独立服务部署重 | — |
| 社区 | 中（Browserbase 维护） | 大（~10 万 star） | 中 | — |

**结论**：
- 执行器内嵌 **Stagehand**（MIT，可修改可商用）
- **Browser Use**（MIT）作为快速对照实验 + 其 `qa-use` 子项目作为 QA 场景参考实现（只读研究，不作依赖）
- **Skyvern 明确不引入依赖**（AGPL 传染），仅作设计参考

**接口缝**：定义 `BrowserAgent` 接口（act / observe / extract / cache / heal），Stagehand 为默认实现；若需更换（如自研 resolver）只动 adapter。

### 2.3 元素识别 —— 三源融合，Vision 兜底

```
Browser State ─→ Screenshot ─┐
              ─→ DOM         ├─→ UI State Model ─→ Agent
              ─→ A11y Tree  ─┘
```

| 层 | 方案 | 说明 |
|---|---|---|
| 主路 | DOM + A11y（Playwright ariaSnapshot） | 覆盖 90% 场景，零额外成本 |
| 语义 | LLM 直接读结构化 snapshot 定位 | Stagehand 已内置 |
| 兜底 | Vision（仅 DOM 不可描述时）：Qwen-VL / UI-TARS 类 | PoC 后再定，第一版可不做 |

**明确不做**：自训 action embedding（QA.tech 2024 路线）。那是产品成熟后的优化，LLM 时代语义定位已被大模型基本覆盖。

### 2.4 爬取/探索层 —— Crawlee + PlaywrightCrawler

- Crawlee（MIT，Apify 出品）+ Playwright 插件：队列、去重、限速、重试开箱即用
- **在其上自研 Exploration Loop**（这是产品核心，不外包给框架）：
  `取页面 → 抽候选动作 → Intent Score 打分 → 选择 → 执行 → 建图记录 → 循环`
- Intent Score 第一版 = LLM 单次打分（prompt 输出 0-100 + 理由），缓存结果

### 2.5 LLM 层 —— 网关统一，模型可插拔

| 用途 | 模型建议 | 说明 |
|---|---|---|
| 探索规划/意图打分 | 国产旗舰（DeepSeek/Qwen 系） | 私有化可用，成本可控 |
| QA 点提取 | 同上，长上下文档 | 配合结构化输出（JSON Schema） |
| 自愈重定位 | 同上小杯 | 高频调用，成本敏感 |
| Vision 兜底 | Qwen-VL 类 | Phase 2+ |

**接入方式**：自建薄网关（OpenAI 兼容协议）→ 上层全走统一 SDK（Vercel AI SDK 或 openai-node）。好处：换模型、双活、内网代理只改一处。
**观测**：Langfuse（MIT，可自部署）记录全部 prompt/response/token/latency/trace。

### 2.6 文档/原型理解层

| 输入 | 方案 | License | 备注 |
|---|---|---|---|
| Word/PDF/MD → 文本 | **MarkItDown**（微软） | MIT | 统一转 Markdown |
| Figma | **Figma 官方 MCP** / REST | 官方 | 拉 Frame/Layer/文本/交互 |
| 飞书文档 | 飞书开放 API（lark-cli 生态） | 官方 | 差异化重点，中文企业刚需 |
| 结构化拆分 | LLM + JSON Schema（需求/角色/规则/验收标准） | — | Requirement Engine 核心 |

### 2.7 前端层

**框架决策：Vue 3 → React 18（V0.2 变更）**

| 对比项 | React 18 ✅ | Vue 3 |
|---|---|---|
| AI 会话/生成式 UI 组件 | **assistant-ui、Vercel AI SDK UI、CopilotKit 等一线组件全是 React 官方**，直接可用 | 仅有社区移植，质量与更新节奏不可控 |
| shadcn 体系 | shadcn/ui 原版 | shadcn-vue 社区移植 |
| 团队适配 | 表单密集型页面 React/Vue 写法差异有限 | 团队现有栈 |
| 生态信号 | Momentic/QA.tech 类产品的控制台大多在 React | — |

> 结论：产品核心竞争力在 AI 会话体验，组件生态的"第一现场"在 React；切换成本主要在团队熟悉度，通过规范模板 + shadcn/ui 降低。原型中的 AI 工作区（灰色气泡/思考折叠/生成式 UI 卡）即按 assistant-ui Thread 范式实现，正式开发直接平移。

| 能力 | 选型 | 理由 |
|---|---|---|
| 框架 | **React 18 + TypeScript（Vite）** | AI 时代组件生态第一现场；私有化单体不需要 Next.js SSR，Vite 更轻 |
| UI 体系 | **shadcn/ui + Tailwind** | 与 QA.tech/Momentic 视觉语言天然一致（白底细边框小圆角）；源码进仓库可完全掌控 |
| **AI 会话** | **@assistant-ui/react** | MIT；Thread/Composer/Reasoning/生成式 UI 组件开箱即用，官方支持 AI SDK 运行时；原型已按其范式验证 |
| 图 | **AntV X6 + @antv/x6-react-shape** | 应用地图/依赖图/覆盖图；MIT；节点用 React 组件渲染 |
| 编辑器 | **Tiptap（@tiptap/react）** | 需求/QA 点富文本；MIT；headless 可做 `/act /assert` 斜杠命令 |
| 实时 | 原生 WebSocket + 轻量状态（**Zustand**） | Agent 事件流驱动 UI；Zustand 足够，不需要引入 rxjs |
| 图表 | ECharts（echarts-for-react） | MIT |

### 2.8 存储层

| 存储 | 用途 | 说明 |
|---|---|---|
| **PostgreSQL 16 + pgvector** | 全部业务数据 + 图结构（邻接表 + 递归 CTE）+ 需求向量检索 | 第一版明确不用 Neo4j；图查询深度 ≤ 5 层时 PG 足够 |
| **Redis 7** | BullMQ 队列 · Agent 会话态 · 定位缓存（可落 PG 持久化） | |
| **MinIO** | 截图/视频/trace.zip/网络归档 | 证据对象存储，带保留策略（如 90 天） |

### 2.9 人机协同通道

- `WAITING_FOR_USER` 事件 → WebSocket 推前端 → 动态表单卡（JSON Schema 驱动，原型已演示）
- **人工接管**：Playwright 浏览器以 headed 模式跑在服务端容器，通过 noVNC/CDP 转发到前端视口；用户操作 = 直接操作同一个 BrowserContext，Agent 暂停监听
- 第一版简化：接管仅支持"用户在弹出窗口操作"（内网单机场景），多租户远期再做

### 2.10 集成层（Phase 3+）

| 集成 | 方式 |
|---|---|
| GitLab（私有化） | Webhook → PR Diff → Impact Analysis → 状态回写 |
| 禅道 / Jira | REST 双向同步 Issue |
| 飞书 | 机器人卡片通知（失败/日报） |
| CI | 提供 `verifyos run --ver ...` CLI（Phase 4） |

### 2.11 移动执行层（Phase 5+ 扩展）

> 对标 QA.tech 第四个产品演示 "Test mobile apps"：Agent 在 Android/iOS 设备上跑原生 App 用例（Launch app → 登录 → 业务操作），环境卡展示 Device/Platform/OS/Viewport，步骤流与 Web 完全同构（时间戳 + 💭 思考 + 子动作），并支持 Fix in chat 修复闭环。VerifyOS 原型已新增 📱 移动测试屏演示该能力。

| 路线 | 选型 | License | 说明 |
|---|---|---|---|
| 移动 Web / App 内 WebView | Playwright（现有底座即可） | Apache-2.0 | **第一版就支持**，零新增组件 |
| Android 原生 | **Maestro** 首选 / Appium+UiAutomator2 备选 | Maestro Apache-2.0 / Appium Apache-2.0 | Maestro 是 YAML 驱动 + 内置 AI 生态（maestro ai），与 Agent 语义贴合 |
| iOS 原生 | Appium+XCUITest（需 macOS 构建机） | Apache-2.0 | 私有化需自备 mac mini，成本前置评估 |

**定位**：移动测试是 Phase 5+ 差异化扩展，不进 Phase 1-4 主线；接口缝预留在 `Execution Worker` —— Run 事件模型（step/思考/证据）设计为平台无关，移动执行器只换 driver 不换数据模型。

### 2.12 工具注册表与插件协议（Tool Registry + MCP）

> 设计判断：**要插件化，但第一版只做"工具注册表 + MCP 协议"，不做插件市场/沙箱/动态加载**。Agent Runtime 本质是"规划 → 调工具 → 观察"循环；db 查询、代码查看、Vision 兜底都是往注册表加条目。要设计的不是插件框架，而是**统一的 Tool 接口规范**。

- **Tool 接口**：`name / description / inputSchema / run(args) / permission`；LLM 侧经 OpenAI 兼容网关转 function calling tools，模型按需自动选择
- **权限三档**：auto（自动执行入证据链）/ ask（走 WAITING_FOR_APPROVAL）/ forbidden——db.exec、vision 大规模调用必须 ask；db.query 强制只读账号 + LIMIT + 超时
- **协议选型**：

| 对比项 | **MCP（Model Context Protocol）** ✅ | 自研插件协议 | OpenAPI 插件 |
|---|---|---|---|
| License | MIT | — | — |
| 生态 | Anthropic/OpenAI/字节等共同支持，现成 server 生态（DB/GitLab/Jira/飞书） | 零 | 仅 REST 描述 |
| 传输 | stdio / SSE（内网友好） | 自定义 | HTTP |
| 与 Agent Runtime 距离 | 官方 TS SDK，一个 adapter 即接入 | 全自研 | 需自写发现层 |

- **第一版内置 8 工具**：browser(Stagehand) / http / db.query(只读) / db.exec(ask) / code.view(GitLab API) / vision(Qwen-VL 兜底, ask) / evidence(MinIO) / report(MR 回写)——详见 PRD §8.4
- **接口缝**：`ToolRegistry.register(tool)`；外部 MCP server 经一个 `McpToolAdapter` 批量转成注册表条目，与内置工具同一调用路径

---

## 3. License 与合规清单

| 组件 | License | 可商用 | 私有化 | 备注 |
|---|---|---|---|---|
| Playwright | Apache-2.0 | ✅ | ✅ | |
| Stagehand | MIT | ✅ | ✅ | 可修改 |
| Crawlee | Apache-2.0 | ✅ | ✅ | |
| shadcn/ui / Tailwind | MIT | ✅ | ✅ | |
| @assistant-ui/react | MIT | ✅ | ✅ | AI 会话组件，官方 React 版 |
| AntV X6（+x6-react-shape） | MIT | ✅ | ✅ | |
| Tiptap core | MIT | ✅ | ✅ | 部分高级扩展付费，不用即可 |
| MarkItDown | MIT | ✅ | ✅ | |
| Langfuse | MIT（核心） | ✅ | ✅ 自部署 | 云版付费不依赖 |
| MCP SDK / 协议 | MIT | ✅ | ✅ | Agent 工具扩展标准协议（PRD §8.4） |
| pgvector / PostgreSQL | PostgreSQL License | ✅ | ✅ | |
| MinIO | AGPL-3.0 ⚠️ | ✅（独立进程、不修改源码、作为服务调用 = 安全） | ✅ | **只作为外部服务部署，绝不 import 其代码**；若法务严格可换 SeaweedFS |
| Skyvern | AGPL-3.0 ❌ | 不引入 | — | 仅设计参考 |
| Vercel AI SDK | Apache-2.0 | ✅ | ✅ | |

> 原则重申：AGPL 组件只能"隔网络调用"或"只读研究"，不能进依赖树、不能改源码分发。

---

## 4. PoC 验证清单（选型落地前必跑）

| # | PoC | 验证目标 | 通过标准 | 预估 |
|---|---|---|---|---|
| P1 | Stagehand act() 在内网 OA（Vue2 老系统）上完成"新增一条审批" | 老旧前端可用性 | 10 次内成功 ≥ 8 | 1 天 |
| P2 | Stagehand cache：改按钮文案后 replay 是否触发 heal | 自愈闭环 | heal 后二次运行免 LLM | 0.5 天 |
| P3 | Crawlee + 登录态 storageState 续爬 | Browser State 复用 | 免重复登录完成 20 页 | 0.5 天 |
| P4 | 国产模型（DeepSeek/Qwen）JSON Schema 结构化输出 QA 点 | 模型可用性 | 50 条 PRD 测试点，可用率 ≥ 60% | 1 天 |
| P5 | Playwright trace.zip 在自建 Viewer 中逐步回放 | 证据链 | 前端能按步骤渲染快照 | 1 天 |
| P6 | Figma MCP 拉 Frame 结构 + 飞书文档 API 读 PRD | 双上下文 | 拿到结构化元素清单 | 1 天 |
| P7 | noVNC 接管：Agent 暂停 → 用户操作 → 恢复 | 人机协同 | 全链路无状态丢失 | 2 天 |
| P8 | PG 递归 CTE 查 300 节点应用地图 5 层子树 | 图在 PG 可行 | < 100ms | 0.5 天 |
| P9 | Langfuse 自部署 + 全链路 trace | 观测 | prompt/token/延迟可见 | 0.5 天 |
| P10 | MinIO 证据生命周期（90 天自动清理） | 存储策略 | 规则生效 | 0.5 天 |
| P11 | @assistant-ui/react 接自建 WebSocket 运行时（事件流 → Thread） | AI 会话层 | 消息/思考折叠/生成式 UI 卡全部渲染 | 1 天 |

**总计 ~9.5 人天**，全部通过后才进入正式开发。

---

## 5. 明确"第一版不用"清单（防过度设计）

| 不用 | 理由 | 何时再考虑 |
|---|---|---|
| LangGraph | 状态机自写更可控，Agent 流程还不稳定 | Agent 流程定型且复杂度超 20 节点 |
| Temporal | BullMQ 够用 | 任务需跨天/人工审批编排时 |
| Neo4j | PG 递归 CTE 覆盖当前查询 | 图查询成为性能瓶颈时 |
| Kafka | 事件量级不到 | 多服务拆分后 |
| 自训 embedding/action 模型 | LLM 语义定位已够 | 有海量标注数据后 |
| 微服务 | 单体迭代最快 | 团队 > 15 人且模块边界稳定 |
| Vision 主路 | 三源融合已覆盖 | P 系列 PoC 证明 DOM 不可描述场景 > 20% 时 |

---

## 6. 风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| Stagehand 对国产 LLM 兼容性差 | 中 | 走 OpenAI 兼容网关适配；P1/P4 PoC 前置；不行就 Browser Use + 自写 cache |
| 老旧前端（ExtJS/iframe 重）A11y 差 | 中 | 三源融合 + iframe 穿透专项测试；保留 Vision 兜底通道 |
| 内网无法访问 Figma/飞书 API | 中 | 支持文件上传兜底路径（MarkItDown 全覆盖） |
| 探索费用失控 | 中 | max_actions 硬上限 + Intent Score 剪枝 + 打分结果缓存 |
| MinIO AGPR 争议（AGPL） | 低 | 作为独立服务调用；法务敏感则换 SeaweedFS（Apache-2.0） |
| Node 单进程 CPU 密集（截图/解析） | 低 | Worker 线程 + 执行器独立进程（D2 拆分条件） |

---

## 7. 技术栈总表（速查）

| 层 | 选型 | License |
|---|---|---|
| 前端框架 | React 18 + TS + Vite | MIT |
| UI | shadcn/ui + Tailwind CSS | MIT |
| AI 会话 | @assistant-ui/react | MIT |
| 图 | AntV X6（x6-react-shape）/ ECharts | MIT |
| 编辑器 | Tiptap（@tiptap/react） | MIT |
| 后端 | NestJS（Node 22） | MIT |
| 任务队列 | BullMQ（Redis） | MIT |
| 浏览器 | Playwright | Apache-2.0 |
| Agent | Stagehand | MIT |
| 工具注册表/插件 | ToolRegistry + MCP | MIT |
| 爬取 | Crawlee | Apache-2.0 |
| 文档 | MarkItDown + Figma MCP + 飞书 API | MIT/官方 |
| LLM | OpenAI 兼容网关（国产模型可插拔） | — |
| LLM 观测 | Langfuse 自部署 | MIT |
| 主存储 | PostgreSQL 16 + pgvector | PostgreSQL |
| 缓存/队列 | Redis 7 | RSAL/SSPL（独立服务使用，无风险） |
| 对象存储 | MinIO（独立服务） | AGPL（仅服务调用） |
| CI/集成 | GitLab Webhook + 禅道/Jira API | — |

---

## 8. 里程碑对应关系

| 阶段 | 新增技术项 | PoC 门槛 |
|---|---|---|
| Phase 1 QA 点提取 | NestJS + PG + MarkItDown + Figma/飞书 + LLM 网关 + Langfuse + React 前端（assistant-ui） | P4, P6, P9, P11 |
| Phase 2 应用探索 | + Playwright + Crawlee + Stagehand + Redis + MinIO | P1, P3, P8, P10 |
| Phase 3 自动执行 | + Stagehand cache/heal + Trace Viewer + noVNC 接管 | P2, P5, P7 |
| Phase 4 PR 验证 | + GitLab Webhook + Impact Analysis + CLI | — |
| Phase 5 移动扩展（规划） | + Maestro/Appium（Android）、移动 Web/WebView（Playwright） | 届时新增 P12/P13 |
